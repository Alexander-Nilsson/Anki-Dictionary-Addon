# Requirements for running Anki Dictionary Addon without full Anki application
# These are the minimal dependencies needed to run the addon directly

# Core Anki components (minimal installation)
anki>=2.1.50

# Qt components for the UI
PyQt6>=6.4.0
PyQt6-WebEngine>=6.4.0

# Additional dependencies that might be needed
requests>=2.25.0
beautifulsoup4>=4.9.0
lxml>=4.6.0

# Optional: For better audio support
pygame>=2.0.0

# Note: You might also need to install Anki's dependencies
# If you encounter import errors, you may need to install:
# pip install anki[qt]