improve code base, particullarly when it comes to handling frequenzy lists, doenst make sense that HSK is treated differently than other lists like TOFL etc. whats a better way to handle this? and allow users to use custom frequenzy lists and setup custom ways to display it. 

Image handling allow users to configer whether they want the images to auto convert (ie causes a little delayl) or not. 

add support for the LLM to return multiple defenitions seperated by some field seperator ex ------ or something. so we can have multiple prompts? 
